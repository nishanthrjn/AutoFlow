# Default: normalize all text files to LF in the repo
* text=auto eol=lf

# Force LF for files that must be Linux-compatible at runtime
infra/mosquitto/config/mosquitto.conf text eol=lf

# Treat binaries as binary (no line-ending conversion)
*.dll binary
*.png binary
*.pdf binary
